To use the tweak server (you can use also your own server)

Setting up the tweak server
1. Install Node from https://nodejs.org/en/download/ (Needs to be done only once)
2. Install template dependencies, navigate to the template root and run: npm install 

Running the server
1. Start server: npm start (from the template root folder)